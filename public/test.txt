p5.inconsolata = p5.loadFont('virtualLand/inconsolata.otf');
    p5.displace = p5.loadShader('virtualLand/shader/displace.vert', 'shader/displace.frag');
    p5.mosaic = p5.loadShader('virtualLand/shader/mosaic.vert', 'shader/mosaic.frag');
    p5.mapId= p5.int(Math.random(0, 3));
    if (p5.mapId == 0) {
      p5.landscape = p5.loadModel('virtualLand/model/cone3.obj');
      p5.uv = p5.loadImage("virtualLand/model/cone3.png");
    } else if (p5.mapId == 1) {
      p5.landscape = p5.loadModel('virtualLand/model/cube3.obj');
      p5.uv = p5.loadImage("virtualLand/model/cube3.png");
    } else if (p5.mapId == 2) {
      p5.landscape = p5.loadModel('virtualLand/model/cross3.obj');
      p5.uv = p5.loadImage("virtualLand/model/cross3.png");
    }
    p5.radPlace=Math.int(Math.random(6));
    p5.radlat = Math.nf(p5.place[p5.radPlace].lat + Math.random(-0.04, 0.04),2,3);
    p5.radlng = Math.nf(p5.place[p5.radPlace].lng+ Math.random(-0.04, 0.04),2,3);
    // p5.console.log(p5.radlat,  p5.radlng );
    const options = {
      lat: radlat,
      lng: radlng,
      zoom: 16,
      width: 640,
      height: 640,
      scale: 1,
      format: 'PNG',
      language: 'en',
      maptype: 'satellite',
    };

    p5.myMap = mappa.staticMap(options);
    p5.img = p5.loadImage(myMap.imgUrl);
    console.log("sussceed");
    p5.source = p5.createGraphics(640, 640);
    p5.noise = p5.loadImage("virtualLand/model/noise.png");
    p5.pg = p5.createGraphics(640, 640);